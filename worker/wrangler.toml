name = "ats-optimizer"
main = "worker.js"
compatibility_date = "2024-09-23"

# Secrets are NOT set here — use `wrangler secret put` for each:
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler secret put ACCESS_PASSWORD
